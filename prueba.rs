fn suma(a: i32, b: i32) -> i32 {
    return a + b;
}
//Comentario de una linea
fn main() {
    let x = 10;
    let y = 20;
//Comentario con acento (áéíóúÁÉÍÓÚ)    
    if x < y {
        let resultado = suma(x, y);
    } else {
        return;
        /*Bloque
        de 
        comentario */
    }
}

/*Bloque de 
comentario
con 
acento (áéíóúÁÉÍÓÚ) */

